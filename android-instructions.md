
# دليل توليد ملف APK الخاص بـ MEMO AI 🚀

لرفع التطبيق على **APKPure**، اتبع هذه الخطوات البسيطة لتحويل الكود الحالي إلى ملف `app-release.apk`:

### الخطوة 1: تجهيز الرابط
تأكد من رفع الكود الحالي على أي استضافة (مثل Vercel أو Netlify) واحصل على الرابط (مثال: `https://your-app.vercel.app`).

### الخطوة 2: استخدام محول PWA إلى APK (الخيار الأسهل)
1. افتح موقع [PWA2APK](https://appmaker.xyz/pwa2apk/) أو [CloudAPK](https://pwa2apk.com/).
2. ضع رابط موقعك.
3. قم برفع أيقونة التطبيق (موجودة في ملفات المشروع).
4. سيقوم الموقع بتوليد ملف الـ **APK** لك خلال دقيقة.

### الخطوة 3: الرفع على APKPure
1. اذهب إلى [APKPure Developer Console](https://developer.apkpure.com/).
2. أنشئ حساب مطور جديد.
3. اختر "Add New App" وارفع ملف الـ APK الذي حصلت عليه.
4. املأ بيانات الوصف (موجودة في ملف `metadata.json`).

### ملاحظة هامة للمتجر:
لقد قمت ببرمجة `Service Worker` و `Manifest` داخل الكود، مما يجعل متجر APKPure يقبل التطبيق كـ "تطبيق أصلي" (Native App) وليس مجرد رابط موقع.
